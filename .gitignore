# Dependencies
node_modules

# Build output
dist
*.tsbuildinfo

# Test output
coverage

# Local-disk file storage (M3-CONTRACT.md §2.2) — no object storage exists yet;
# generated PDFs land here in dev/CI and must never be committed.
/storage/

# Environment — never commit real secrets, only .env.example
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea
.vscode
